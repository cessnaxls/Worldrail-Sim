WorldRail iPad Black Screen Repair

Root cause fixed:
drive3d.js passed the numeric throttle state variable into lever() rather than the #throttle DOM control.
That threw a JavaScript TypeError while the script was initializing, before WorldRailDrive was registered.

Additional Safari hardening:
- explicit getElementById references for every driving control/HUD element
- renderer/CDN failure diagnostic overlay
- duplicate render-loop guard
- visible renderer boot indicator
- existing 30–40 FPS streaming/dynamic-resolution architecture preserved
- full 520-object library preserved
