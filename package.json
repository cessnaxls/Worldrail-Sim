{
  "name": "worldrail-hmmsim-studio",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.mjs",
    "start": "vite preview --host 0.0.0.0 --port $PORT"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}