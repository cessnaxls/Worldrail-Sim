{
  "name": "worldrail-hmmsim-studio",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.mjs",
    "start": "npx vite preview --host 0.0.0.0 --port ${PORT:-10000}"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}